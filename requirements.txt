# LIBS Foundation Model Dependencies
# 
# RECOMMENDED: Use uv with setup scripts (setup_env.ps1 or setup_env.sh)
# These handle CUDA-enabled PyTorch installation properly.
#
# ALTERNATIVE: Manual pip install with CUDA
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt

# Core ML (PyTorch must be installed separately with CUDA - see above)
pytorch-lightning>=2.0.0

# Data handling
numpy>=1.24.0
h5py>=3.8.0
pandas>=2.0.0

# Configuration
pyyaml>=6.0
omegaconf>=2.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.2.0

# Logging
tensorboard>=2.12.0

# Utilities
tqdm>=4.65.0
